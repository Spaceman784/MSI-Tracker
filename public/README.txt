Put your NapChief logo here as:

    logo.png

(this folder = MSI Report/public/)

Once logo.png is here, it shows automatically in the login page and the
dashboard header. Until then, the app shows the "N" badge as a fallback.

Tip: a PNG with a transparent background looks best, but a white-background
image works too (it sits on a white rounded chip).
